{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "npm start",
            "type": "shell",
            "command": "bash -c 'source ~/.nvm/nvm.sh && nvm use 19 && npm start'",
            "options": {
                "cwd": "${workspaceFolder}/example"
            },
            "problemMatcher": []
        }
    ]
}
