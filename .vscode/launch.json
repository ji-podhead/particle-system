{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CRA in Chrome",
            "type": "firefox",
            "request": "launch",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}/example",
            "preLaunchTask": "npm start"
        }
    ]
}
